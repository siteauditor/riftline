# Copied to /root/umami/.env on the server and filled in there; never
# committed. Both values are generated once (`openssl rand -hex 32`) and
# never need to be known by a person: the database is reachable only from
# the umami container, and APP_SECRET only signs Umami's own sessions.
POSTGRES_PASSWORD=
APP_SECRET=
