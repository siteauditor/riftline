# Secrets: the Riot key lives here and must never be committed.
.env
.env.local
*.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Internal docs: planning notes kept on the workstation, not published with the
# repository. Ignoring a file only stops future tracking: anything listed here
# that was committed before it was listed is still in the history.
docs/feature-gap-analysis.md

# Data: the match corpus and Data Dragon cache are both rebuildable.
data/
*.db
*.db-wal
*.db-shm

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
