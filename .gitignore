# Secrets: the Riot key lives here and must never be committed.
.env
.env.local
*.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Internal docs: planning notes kept on the workstation, not published with the
# repository. The whole folder, so a new internal doc is private by default.
#
# Nothing in it has ever been committed, which also means switching to an older
# commit cannot delete it. The one exception to "never committed" is
# feature-gap-analysis.md, which lived in docs/ until 2026-09-18 and is still in
# the history from before then.
internal-docs/

# Data: the match corpus and Data Dragon cache are both rebuildable.
data/
*.db
*.db-wal
*.db-shm

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
